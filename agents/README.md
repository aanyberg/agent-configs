# Moved

Agents now live at [`plugins/conventions/agents`](../plugins/conventions/agents), packaged as part of the `conventions` Claude Code plugin.

See [docs/CONSUMER.md](../docs/CONSUMER.md) for how a consumer repo loads them via the plugin marketplace instead of symlinking this directory.
